import Typed from 'typed.js';

var options = {
  strings: ["Trouvez un freelancer ", "Pour votre future projet." ,'En développement mobile, web...'],
  typeSpeed: 50,
  loop:true
}

var typed = new Typed(".typed", options);